services:
  wx-md:
    # build: ..
    image: your-repo/wx-md:latest
    container_name: wx-md
    ports:
      - "9088:80"
    # volumes:
    #   - ../nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: always